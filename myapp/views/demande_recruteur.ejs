<!DOCTYPE html>
<html>
  <head>
    <script src="/assets/js/color-modes.js"></script>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/stylesheets/style.css" rel="stylesheet">
    <link href="/assets/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Demande pour devenir recruteur</title>
    <style>
      body { background: #f8f9fa; }
      .card-demande {
        max-width: 500px;
        margin: 40px auto;
        border-radius: 18px;
        box-shadow: 0 4px 24px rgba(0,0,0,0.08);
        background: #fff;
        padding: 2.5rem 2rem 2rem 2rem;
      }
      .card-demande h1 {
        font-size: 1.7rem;
        font-weight: 700;
        color: #0d6efd;
      }
      .form-floating label { left: 0.75rem; }
      .btn-primary {
        background: linear-gradient(90deg, #2563eb 0%, #1d4ed8 100%);
        border: none;
        font-weight: 600;
        letter-spacing: 0.5px;
      }
      .logo-demande {
        display: block;
        margin: 0 auto 1.2rem auto;
        width: 64px;
        height: 64px;
      }
    </style>
  </head>
  <body>
<%- include('component/header', {
      pageTitle: 'Demande pour devenir recruteur',
      current: 'demande',
      prenom: prenom, nom: nom, role: role
    }) %>

<main>
  <div class="card-demande">
    <img src="/assets/brand/bootstrap-logo.svg" alt="Logo" class="logo-demande">
    <h1 class="mb-4 text-center">Demande pour devenir recruteur</h1>
    <form action="/utilisateurs/demande_recruteur" method="post">
      <div class="form-floating mb-4">
        <select class="form-select" id="organisation" name="siren" required aria-label="Organisation">
          <option value="" disabled selected>Choisissez votre organisation</option>
          <% organisations.forEach(orga => { %>
            <option value="<%= orga.siren %>">
              <%= orga.nom %> – <%= orga.adresse %>
            </option>
          <% }) %>
        </select>
        <label for="organisation">Organisation</label>
      </div>
      <button class="w-100 btn btn-lg btn-primary" type="submit">
        <i class="bi bi-send me-2"></i>Envoyer la demande
      </button>
    </form>
  </div>
</main>
<%- include('component/footer') %>
  </body>
</html>